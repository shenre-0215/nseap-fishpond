# Suspension Layer + Small Web: Dynamic Ontology Growth for Safe AI

> 悬置层与小网——基于 NSEAP 平台的动态本体生长与安全探索验证实验

## 🔥 v2 Update (2026-06-02)

**论文重构 + 消融实验：动态本体生长是结构性的必要条件。**

### What's new

| 新增 | 说明 |
|------|------|
| **paper-v2.md** | 论文重构——从"复杂系统安全学习"转为"动态本体生长+安全探索"，桥接神经符号AI和安全RL两个社区 |
| **消融实验 1** | NSEAP vs NSEAP-Static（静态预定义本体） |
| **消融实验 2** | NSEAP vs NSEAP-NoSusp vs NSEAP-NoClose（组件移除） |

### 消融实验核心发现

| 实验 | 崩溃率 | 收获 | 结论 |
|------|--------|------|------|
| NSEAP（完整） | **0%** | 13,039 | 安全+高产 |
| NSEAP-Static（无动态生长） | 0% | **0** | ⚠️ 完全瘫痪 |
| NSEAP-NoSusp（无悬置层） | **100%** | 693 | ❌ 3.6周期崩溃 |
| NSEAP-NoClose（无闭环） | 0% | 13,039 | 一维环境无独立效应 |

**组件贡献层级：**
1. **动态本体生长** → 没有它 = 瘫痪（0收获）
2. **悬置层** → 没有它 = 必死（100%崩溃）
3. **闭环检测** → 价值随环境复杂度增长

### 快速运行 v2 实验

```bash
# 第一组：静态本体消融
python main_ablation.py --runs 200

# 第二组：组件移除消融
python main_ablation2.py --runs 200
```

### 科学方向

**神经符号 AI × 安全 RL 交叉地带**：首次同时回应——

- 神经符号AI的"本体从哪来"（小网动态生长）
- 安全RL的"未知环境安全探索"（拓扑闭环验证）

详情见 [paper-v2.md](paper-v2.md)

---

## 核心思想

NSEAP 是一种面向**复杂系统决策**的智能体架构，核心观点是：

在充满不确定性、非线性、临界态的复杂系统中，传统的强化学习（如 Q-Learning）存在两个根本问题：
1. **探索即崩溃**：在临界系统中，随机探索可能直接导致系统崩溃，无法完成学习
2. **预设本体偏见**：预定义的状态/动作空间无法适应开放环境中不断涌现的新知识

NSEAP 通过三层架构解决这个问题：

```
感知 → 悬置层 → 小网推理 → 决策 → 执行 → 校准 → 闭环固化 → ...
```

### 1. 悬置层 (Suspension Layer)

插入在**感知**和**推理**之间：
- 不做预设判断（不直接输出"鱼多了应该多捕"这种结论）
- 只提取**中性属性**：当前数量、趋势、变化率、到已知危险边界的距离、不确定性
- 列出所有候选行动，但不推荐任何一个
- 将决策权交给小网推演

### 2. 小网 (Small Web)

关系优先的**动态本体**：
- 节点 = 状态区间（如鱼群数量"0-100", "100-300"...）
- 边 = 行动 → 下一状态的关系转移
- 边不是预定义的，而是在交互中**动态生长**
- 每条边有置信度，只有经过**闭环验证**才能固化为知识

### 3. 实用闭环 (Practical Closure)

避开哥德尔不完备性陷阱：
- 有限深度推演（默认最大深度 5 步）
- 外部世界校准（预测 vs 实际观察）
- 只有能闭环回到已知安全节点的路径才被确认

## 验证场景：鱼塘生态管理

你管理一个鱼塘，每个周期决定捕捞多少鱼。

- 鱼群按 Logistic 曲线自然增长
- 存在**未知临界点**：跌破阈值后不可逆崩溃
- 智能体初始对临界点、增长率一无所知
- 目标：**在不崩溃的前提下最大化累计捕捞量**

### 环境参数（默认）

```python
carrying_capacity = 1000    # 环境承载力
growth_rate = 0.3           # 自然增长率
critical_threshold = 150    # 崩溃临界点
noise = 0.05                # 增长随机噪声
```

### 对比智能体

| 智能体 | 策略 | 说明 |
|--------|------|------|
| Greedy | 每次捕当前的 50% | 贪心基准，没有学习 |
| Conservative | 每次固定捕 50 条 | 保守基准，固定策略 |
| QLearning | 离散状态 Q 表更新 | 标准强化学习 |
| **NSEAP** | 悬置层 + 小网闭环 | 本方法 |

### 单智能体实验结果（100 次运行 × 200 周期）

| Agent | Avg Survival Cycles | Avg Total Harvest | Collapse Rate |
|-------|---------------------|-------------------|---------------|
| Greedy | 4.2 | 728 | **100%** |
| Conservative | 200.0 | 10,000 | 0% |
| QLearning | 139.1 | 9,193 | 50% |
| **NSEAP** | **200.0** | **13,059** | **0%** |

### 结果分析

- **贪心**：短期收益高，但几乎必然触碰到临界点崩溃
- **保守固定**：永不崩溃，但收益很低，远低于可持续产出上限
- **Q-Learning**：需要探索，探索过程中约一半概率崩溃
- **NSEAP**：
  - 通过逐步试探识别出安全边界
  - **零崩溃**，同时收获比保守策略高 **30%**
  - 在安全边界附近最大化产出

### 参数敏感性测试

| 条件 | NSEAP | Conservative | QLearning | 结论 |
|------|-------|--------------|-----------|------|
| 噪声翻倍 (0.1) | 0% 崩溃, 收获 +28% vs 保守 | 0% 崩溃 | 58% 崩溃 | NSEAP 鲁棒性保持 |
| 增长率降低 (0.2) | 0% 崩溃, 收获 +22% vs 保守 | 12% 崩溃 | 67% 崩溃 | 系统更脆弱时，NSEAP 优势更大 |

### 自我修正能力验证（真实临界点 = 150）

| 初始安全边界猜测 | NSEAP 崩溃率 | 最终平均边界 | 结果 |
|-----------------|-------------|--------------|------|
| 100（猜低，偏激进） | **0%** | **150.0** | ✅ 完美自我修正，自动调整到真实临界点 |
| 150（正确） | 0% | 150.0 | 正常工作 |
| 250（猜高，偏保守） | 0% | 250.0 | 保持保守，零崩溃零收获 |
| 400（严重猜高） | 0% | 400.0 | 保持保守，零崩溃零收获 |

**结论**：当初始猜测偏激进，NSEAP 能够通过探索自动修正到正确边界，仍然保持零崩溃，验证了动态本体修正能力。

## 扩展场景：两智能体公共资源博弈（公地悲剧）

两个智能体共享同一个鱼塘，各自决策捕捞量。

- 总承载力 = 2000
- 总临界点 = 300
- 每个智能体只能观察到自己的捕捞量和当前总鱼群
- 公地悲剧：个体贪心 → 集体过度捕捞 → 系统崩溃

### 实验结果（100 次运行 × 200 周期）

| Combination | Avg Cycles | Avg Total Harvest (Both) | Collapse Rate |
|-------------|------------|--------------------------|---------------|
| Greedy vs Greedy | 1.0 | 1,203 | **100%** |
| Conservative vs Conservative | 200.0 | 20,000 | 0% |
| QLearning vs QLearning | 34.5 | 5,170 | **100%** |
| **NSEAP vs NSEAP** | **200.0** | **0** | **0%** |

### 结果解读

- **贪心 vs 贪心**：双方都最大化自己短期收益，第一周期就崩盘，完美演绎公地悲剧
- **保守 vs 保守**：双方都固定低捕捞，永久可持续，但总收益很低
- **QLearning vs QLearning**：探索过程中必然过度捕捞，几乎 100% 崩溃
- **NSEAP vs NSEAP**：
  - 初始不确定性极高 → 双方都选择**不捕捞**（最保守行动）
  - 结果：**零崩溃**，但总收获为零
  - 这完全符合 NSEAP 的设计逻辑：**不确定性超过阈值时，优先保证系统不崩溃，哪怕收益为零**
  - 如果调低初始不确定性阈值，NSEAP 会更早开始试探，可以得到更高的收获，但崩溃风险也会上升

## 安装与运行

### 依赖

- Python 3.8+
- matplotlib
- numpy

```bash
pip install matplotlib numpy
```

### 运行单智能体实验

```bash
python main.py --runs 100 --cycles 200
```

结果保存在 `results/` 目录：
- `summary.json`: 汇总数据
- `comparison.png`: 结果对比图

### 运行两智能体公共资源博弈实验

```bash
python main_two_agent.py --runs 100 --cycles 200
```

结果保存在 `results_two_agent/` 目录。

### 参数调整

你可以修改环境参数测试不同条件下的表现：

```python
# 在 environment.py 中修改
class FishPond:
    def __init__(self, carrying_capacity=1000, growth_rate=0.3, 
                 critical_threshold=150, noise=0.05):
        ...
```

修改 NSEAP 参数：

```python
# 在 nseap.py 中修改
class NSEAPAgent:
    def __init__(self, max_depth=5, confirm_threshold=3):
        # max_depth: 闭环检测最大深度
        # confirm_threshold: 需要多少次成功验证才能固化边
        ...
```

## 项目结构

```
fishpond/
├── README.md                    # 本文件（含 v2 更新）
├── paper.md                     # v1 论文（单/两智能体实验）
├── paper-v2.md                  # v2 论文（动态本体生长+安全探索）
├── PROJECT_HISTORY.md           # 完整开发历史
├── environment.py               # 单智能体鱼塘环境模拟器
├── two_agent_env.py             # 两智能体共享鱼塘环境
├── suspension.py                # 悬置层实现
├── small_web.py                 # 小网（动态关系图）实现
├── agents/
│   ├── greedy.py                # 贪心智能体
│   ├── conservative.py          # 保守固定智能体
│   ├── qlearning.py             # Q-Learning 强化学习智能体
│   ├── nseap.py                 # NSEAP 智能体
│   ├── nseap_static.py          # [v2] 静态本体消融变体
│   └── nseap_ablations.py       # [v2] 组件移除消融变体
├── experiment.py                # 单智能体实验运行器
├── experiment_two_agent.py      # 两智能体实验运行器
├── experiment_ablation.py       # [v2] 静态本体消融实验
├── experiment_ablation2.py      # [v2] 组件移除消融实验
├── visualize.py                 # 可视化
├── main.py                      # 单智能体实验入口
├── main_two_agent.py            # 两智能体实验入口
├── main_ablation.py             # [v2] 静态本体消融入口
└── main_ablation2.py            # [v2] 组件移除消融入口
```

## 核心结论

1. **动态本体生长是结构性必要条件**（v2 消融发现）：静态预定义本体导致完全瘫痪——预定义边全是自环，陷入死锁
2. **悬置层是第一性安全机制**（v2 消融发现）：移除后 100% 崩溃，平均存活 3.6 周期
3. **NSEAP 在复杂临界系统中比传统强化学习更鲁棒**：通过保守试探 + 闭环验证，0% 崩溃，收获比保守策略高 30%
4. **悬置层有效延迟了本体承诺**：不预设"什么是对的"，从交互中长出本体
5. **有限闭环避开不完备性**：不需要全局一致性，能闭环回安全节点即可
6. **不确定性管理优先**：宁可不收获，也不冒未知风险

## 理论联系

NSEAP 的思想和以下理论有深刻关联：

- **神经符号AI**：首次实现了本体从交互中动态生长，而非专家预设（v2 新增）
- **安全强化学习**：拓扑闭环验证替代预定义安全约束（v2 新增）
- **自由能原理**：实用闭环本质上是自由能最小化的拓扑近似
- **预测加工理论**：悬置层对应大脑"暂停等待预测误差澄清"的机制
- **复杂系统临界性**：临界点附近行为剧变，传统探索容易崩溃
- **动态本体论**：本体在交互中进化生长

## 未来扩展

- [x] ~~消融实验：动态本体 vs 静态本体~~ ✅ 2026-06-02
- [x] ~~消融实验：组件移除（悬置层、闭环检测）~~ ✅ 2026-06-02
- [ ] 扩展到高维状态空间，验证闭环检测独立价值
- [ ] 对比深度强化学习/安全强化学习方法
- [ ] 实现跨领域函子映射（范畴论形式化）
- [ ] 测试 N 智能体 (>2) 公共资源博弈
- [ ] 将 NSEAP 应用到其他复杂决策场景
- [ ] 提交 arXiv preprint
- [ ] 投稿 AAAI / IJCAI / NeSy Workshop

## 许可证

MIT

## 作者

**悬置层与小网理论原创**: [shen](https://github.com/shenre-0215)

平台实现（基于 NSEAP 框架）与实验: Claude Code
